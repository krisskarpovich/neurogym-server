# регистрация без автоматического входа
# @router.post("/register", response_model=UserResponse)
# def register(user_data: UserCreate, db: Session = Depends(get_db)):
#     # Проверка уникальности
#     existing = db.query(User).filter(User.email == user_data.email).first()
#     if existing:
#         raise HTTPException(status_code=400, detail="Email already registered")

#     # Хешируем пароль
#     hashed_pw = hash_password(user_data.password)

#     # Создаём пользователя
#     new_user = User(
#         email=user_data.email,
#         hashed_password=hashed_pw,
#         first_name=user_data.first_name,
#         last_name=user_data.last_name,
#         weight=user_data.weight,
#         height=user_data.height,
#         birth_date=user_data.birth_date,
#     )
#     db.add(new_user)
#     db.commit()
#     db.refresh(new_user)

#     # Вычисляем BMI (если данные есть)
#     if new_user.weight and new_user.height:
#         bmi = round(new_user.weight / ((new_user.height / 100) ** 2), 2)
#     else:
#         bmi = 0

#     return UserResponse(
#         id=new_user.id,
#         email=new_user.email,
#         first_name=new_user.first_name,
#         last_name=new_user.last_name,
#         bmi=bmi,
#     )


/https://neurogym_backend.com/uploads/icons/biceps.png